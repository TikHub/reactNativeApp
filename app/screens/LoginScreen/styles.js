import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    // padding: 10,
  },
  logo: {
    height: 80,
    width: 80,
    alignSelf: "center",
  },
});

export default styles;
